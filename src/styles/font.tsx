import { StyleSheet } from "react-native";

const font = StyleSheet.create({
  title: { color: "grey", fontSize: 30 },
  subtitle: { color: "grey", fontSize: 30 },
  label: { color: "grey", fontSize: 20 },
  text: {},
});

export default font;
